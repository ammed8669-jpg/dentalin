# 🧾 نظام الفواتير للمبيعات - Sales Invoice System

نظام فواتير متكامل لمندوبي المبيعات يعمل بدون الحاجة إلى Google Sheets API الرسمي.

## ✨ المميزات

- 📊 جلب البيانات مباشرة من Google Sheets المنشور
- 🔍 بحث وتصفية متقدمة للمنتجات
- 🏷️ تصنيف المنتجات حسب المجموعات
- 📦 إدارة المخزون تلقائياً
- 🧾 إنشاء فواتير احترافية
- 📤 تصدير الفواتير بصيغ متعددة (PDF/HTML, Excel/CSV, JSON)
- 📱 تصميم متجاوب يعمل على جميع الأجهزة
- 🎨 واجهة مستخدم جميلة وسهلة الاستخدام

## 🚀 كيفية الاستخدام

### 1️⃣ إعداد Google Sheet

1. أنشئ Google Sheet جديد بالأعمدة التالية:
   ```
   Group | Product Name | Available Qty | Unit Price
   ```

2. أمثلة على البيانات:
   ```
   إلكترونيات | لابتوب HP ProBook | 15 | 3500
   مكتبيات | دفتر ملاحظات A4 | 200 | 15
   أثاث | كرسي مكتب مريح | 25 | 800
   ```

3. نشر الجدول:
   - اذهب إلى `File` > `Share` > `Publish to web`
   - اختر `Entire Document` أو الورقة المطلوبة
   - اختر `Comma-separated values (.csv)`
   - انقر على `Publish`
   - انسخ الرابط

4. استبدل رابط Google Sheet في الملف `src/services/sheetService.ts`:
   ```typescript
   const SHEET_URL = 'YOUR_GOOGLE_SHEET_CSV_LINK_HERE';
   ```

### 2️⃣ تشغيل التطبيق

```bash
# تثبيت المكتبات
npm install

# تشغيل وضع التطوير
npm run dev

# بناء المشروع للإنتاج
npm run build
```

### 3️⃣ استخدام التطبيق

1. **اختيار المجموعة**: اختر مجموعة المنتجات من القائمة المنسدلة
2. **البحث**: ابحث عن منتج معين باستخدام حقل البحث
3. **إضافة للفاتورة**: أدخل الكمية المطلوبة واضغط "إضافة"
4. **تعديل الفاتورة**: يمكنك تعديل الكميات أو حذف عناصر من الفاتورة
5. **تصدير**: اختر طريقة التصدير المناسبة (PDF/HTML, Excel, JSON)

## 📋 البيانات التجريبية

التطبيق يحتوي على بيانات تجريبية للاختبار تشمل:
- **إلكترونيات**: لابتوب، ماوس، كيبورد، شاشة، سماعات
- **مكتبيات**: دفاتر، أقلام، مجلدات، آلة حاسبة، مقص
- **أثاث**: كراسي، طاولات، خزانات، رفوف
- **طباعة**: طابعات، حبر، ورق، ماسح ضوئي

## 🛠️ التقنيات المستخدمة

- **React 19** - مكتبة بناء الواجهات
- **TypeScript** - للكتابة الآمنة
- **Tailwind CSS** - للتصميم
- **Vite** - أداة البناء السريعة

## 📦 هيكل المشروع

```
src/
├── components/          # المكونات
│   ├── GroupSelector.tsx
│   ├── SearchBar.tsx
│   ├── ProductList.tsx
│   ├── InvoiceTable.tsx
│   └── ExportButtons.tsx
├── services/           # الخدمات
│   └── sheetService.ts
├── types/             # تعريفات الأنواع
│   └── index.ts
├── utils/             # الأدوات المساعدة
│   └── exportInvoice.ts
└── App.tsx           # المكون الرئيسي
```

## 🔧 التخصيص

### تغيير العملة
في الملفات `src/components/InvoiceTable.tsx` و `src/utils/exportInvoice.ts`، ابحث عن `ر.س` واستبدلها بالعملة المطلوبة.

### تغيير الألوان
الألوان الرئيسية تستخدم `indigo` و `purple`. يمكنك تغييرها في جميع المكونات.

### إضافة حقول جديدة
أضف الحقول الجديدة في:
1. `src/types/index.ts` - تعريف النوع
2. `src/services/sheetService.ts` - قراءة البيانات
3. المكونات المطلوبة - عرض البيانات

## ⚠️ ملاحظات مهمة

1. **Google Sheet يجب أن يكون منشوراً للعامة** لقراءة البيانات
2. التطبيق يعمل فقط في **القراءة** من Google Sheets، لا يقوم بالكتابة
3. تحديث المخزون يحدث فقط في **الواجهة المحلية**، ولا يتم حفظه في Google Sheets
4. للكتابة على Google Sheets ستحتاج استخدام API الرسمي

## 🔐 الأمان

- لا تضع معلومات حساسة في Google Sheet المنشور
- استخدم هذا النظام للبيانات العامة فقط
- للبيانات الحساسة، استخدم Google Sheets API مع مصادقة OAuth

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تأكد من نشر Google Sheet بشكل صحيح
2. تحقق من تنسيق البيانات (الأعمدة الأربعة المطلوبة)
3. افتح Developer Console في المتصفح لرؤية الأخطاء

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

**تم تطويره بـ ❤️ باستخدام React و TypeScript**
